extends TextureRect


func _draw() -> void:
	#var image: Image = Image.create_empty(size.x, size.y, false, Image.Format.FORMAT_RGB8)
	
	#for x in range(size.x):
	#	for y in range(size.y):
	#		image.set_pixel(x, y, ColorConversions.xyToColor([x / size.x, y / size.y]))
	
	#texture = ImageTexture.create_from_image(image)
	pass
